CREATE TABLE [dbo].[bigint_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    BIGINT
);
CREATE TABLE [dbo].[int_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    INT
);
CREATE TABLE [dbo].[smallint_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    SMALLINT
);
CREATE TABLE [dbo].[tinyint_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    TINYINT
);

