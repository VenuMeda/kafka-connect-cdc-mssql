CREATE TABLE [dbo].[smallmoney_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    SMALLMONEY
);
CREATE TABLE [dbo].[money_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    MONEY
);

