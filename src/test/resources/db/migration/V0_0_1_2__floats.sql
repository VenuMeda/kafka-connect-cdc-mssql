CREATE TABLE [dbo].[float_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    FLOAT
);
CREATE TABLE [dbo].[real_table] (
  id         BIGINT IDENTITY PRIMARY KEY NOT NULL,
  [value]    REAL
);

